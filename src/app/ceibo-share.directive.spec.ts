import { CeiboShareDirective } from './ceibo-share.directive';

describe('CeiboShareDirective', () => {
  it('should create an instance', () => {
    const directive = new CeiboShareDirective();
    expect(directive).toBeTruthy();
  });
});
